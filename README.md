# Retina Clinic

Proposta de reformulação de site — estudo interno, **não contratado** e sem
vínculo com a empresa. Publicado apenas para apresentação.

Site estático, sem build no servidor. Publicado via GitHub Pages a partir da
branch `main`. As páginas trazem `noindex` e o `robots.txt` bloqueia rastreio:
a proposta não deve concorrer com o site atual do cliente nos buscadores.
